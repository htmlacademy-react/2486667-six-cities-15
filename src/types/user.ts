export type User = {
  name: string;
  avatarUrl: string;
  isPro: boolean;
}

export type AuthData = {
  login: string;
  password: string;
}

export type UserData = {
  email: string;
  token: string;
  name: string;
  avatarUrl: string;
  isPro: boolean;
}
